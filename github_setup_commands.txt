# GitHub Setup Commands
# Run these commands after creating the GitHub repository

# Add the GitHub repository as remote origin (replace USERNAME with your GitHub username)
git remote add origin https://github.com/USERNAME/rentalyze.git

# Push the code to GitHub
git push -u origin main

# Verify the push was successful
git remote -v